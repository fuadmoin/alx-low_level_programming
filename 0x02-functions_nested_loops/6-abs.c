#include "main.h"
#include <stdio.h>

/**
 * _abs - Checks if c is a alphabet letter.
 * @n: int - character to be checked.
 * Return: absolute value of n.
 */

int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
